class _showAddTaskDialog {
  String newTask = '';
  showDialog(
      context: context,
      builder: (context) {

        }
      return AlertDialog(
      title: Text("Add New Task"));
      )
async }